{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT", "../../.yarn/versions/*"],
      "outputs": ["esm/**", "cjs/**"]
    },
    "lint": {
      "dependsOn": ["build"]
    },
    "format": {
      "dependsOn": ["build"]
    }
  }
}
