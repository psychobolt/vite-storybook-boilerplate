name: Packages

on:
  push:
    paths:
      packages/**
  workflow_dispatch:
    inputs:
      change-from:
        type: string

jobs:
  diff:
    uses: ./.github/workflows/diff.yml

  html-ui:
    needs: diff
    uses: ./.github/workflows/packages-test.yml
    if: contains(fromJSON(needs.diff.outputs.package_names), 'html-ui')
    with:
      workspace: html-ui
      path: packages/html-ui
    secrets: inherit

  react-ui:
    needs: diff
    if: contains(fromJSON(needs.diff.outputs.package_names), 'react-ui')
    uses: ./.github/workflows/packages-test.yml
    with:
      workspace: react-ui
      path: packages/react-ui
    secrets: inherit