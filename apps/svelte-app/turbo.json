{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "dev": {
      "dependsOn": ["html-ui#build"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".svelte-kit/**"]
    }
  }
}