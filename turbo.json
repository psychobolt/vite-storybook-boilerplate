{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/yarn.lock", ".yarn/versions/*"],
  "pipeline": {
    "//#bootstrap": {
      "cache": false
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "watch": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "build": {
      "dependsOn": ["^build"]
    },
    "build-storybook": {
      "outputs": ["storybook-static/**"],
      "dependsOn": ["^build"]
    },
    "//#config-ci": {
      "env": ["CI"],
      "inputs": [
        "!**/node_modules",
        "**/.yarn/patches/**",
        "**/yarn.lock",
        "bitbucket-pipelines.yml"
      ],
      "outputs": ["bitbucket-pipeline.yml"]
    },
    "config-ci": {},
    "//#lint": {
      "dependsOn": ["^build"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "test": {},
    "coverage": {},
    "lcov": {
      "inputs": ["coverage/coverage*(*).json"],
      "outputs": ["coverage/**/lcov.info", "coverage/**/lcov-report/**"]
    },
    "//#format": {},
    "format": {},
    "chromatic": {
      "inputs": ["storybook-static/**"],
      "dependsOn": ["build-storybook"]
    }
  }
}
